{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<div class="login">
  <div class="form">
    <div>
      <a class=" btn btn-lg btn-primary" href="{{path('app_login')}}">Connexion</a>
    </div>
    <div>
      <a class=" btn btn-lg btn-primary" href="{{path('app_register')}}">S'enregistrer</a> 
    </div>
  </div>
</div>
{% endblock %}
